<% layout("/layouts/boilerplate") %>

<!-- jiya: My Listings page for users to manage their own listings -->

<div class="d-flex justify-content-between align-items-center flex-wrap gap-2 mt-3">
  <div>
    <h3 class="mb-0">My Listings</h3>
    <p class="text-muted mb-0" style="font-size: 0.9rem;">
      Manage your property listings. All listings require admin approval before being published.
    </p>
  </div>
  <div>
    <a href="/listings/new" class="btn btn-primary">
      <i class="bi bi-plus-circle"></i> Add New Listing
    </a>
  </div>
</div>

<% if (allListing.length === 0) { %>
  <div class="alert alert-info mt-4">
    <h5 class="alert-heading">No listings yet</h5>
    <p class="mb-0">You haven't created any listings yet. Click "Add New Listing" to get started!</p>
  </div>
<% } else { %>
  <div class="row mt-4">
    <% allListing.forEach(listing => { %>
      <div class="col-md-6 col-lg-4 mb-4">
        <div class="card h-100">
          <% if (listing.image && listing.image.length > 0) { %>
            <img src="<%= listing.image %>" class="card-img-top" alt="<%= listing.title %>" style="height: 200px; object-fit: cover;">
          <% } else { %>
            <div class="card-img-top bg-light d-flex align-items-center justify-content-center" style="height: 200px;">
              <i class="bi bi-image text-muted" style="font-size: 3rem;"></i>
            </div>
          <% } %>
          
          <div class="card-body d-flex flex-column">
            <h5 class="card-title"><%= listing.title %></h5>
            <p class="card-text text-muted small">
              <i class="bi bi-geo-alt"></i> <%= listing.location %>
            </p>
            <p class="card-text"><%= listing.description ? listing.description.substring(0, 100) + '...' : '' %></p>
            
            <!-- jiya: Approval status badge -->
            <div class="mb-2">
              <% if (listing.approvalStatus === 'pending') { %>
                <span class="badge bg-warning">Pending Approval</span>
              <% } else if (listing.approvalStatus === 'approved') { %>
                <span class="badge bg-success">Approved</span>
              <% } else if (listing.approvalStatus === 'rejected') { %>
                <span class="badge bg-danger">Rejected</span>
              <% } %>
            </div>
            
            <div class="mt-auto">
              <p class="card-text fw-bold text-primary">
                $<%= listing.price %> <span class="text-muted fw-normal">/night</span>
              </p>
              
              <div class="btn-group w-100" role="group">
                <a href="/listings/<%= listing._id %>" class="btn btn-outline-primary btn-sm">
                  <i class="bi bi-eye"></i> View
                </a>
                <% if (listing.approvalStatus === 'pending' || listing.approvalStatus === 'rejected') { %>
                  <a href="/listings/<%= listing._id %>/edit" class="btn btn-outline-secondary btn-sm">
                    <i class="bi bi-pencil"></i> Edit
                  </a>
                <% } %>
                <form action="/listings/<%= listing._id %>?_method=DELETE" method="POST" class="d-inline" 
                      onsubmit="return confirm('Are you sure you want to delete this listing?');">
                  <button type="submit" class="btn btn-outline-danger btn-sm">
                    <i class="bi bi-trash"></i> Delete
                  </button>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    <% }) %>
  </div>
<% } %>

<!-- jiya: Status information -->
<div class="mt-4">
  <div class="card">
    <div class="card-header">
      <h6 class="mb-0">Listing Status Guide</h6>
    </div>
    <div class="card-body">
      <div class="row">
        <div class="col-md-4">
          <span class="badge bg-warning">Pending Approval</span>
          <p class="small text-muted mt-1 mb-0">Your listing is waiting for admin review.</p>
        </div>
        <div class="col-md-4">
          <span class="badge bg-success">Approved</span>
          <p class="small text-muted mt-1 mb-0">Your listing is live and visible to users.</p>
        </div>
        <div class="col-md-4">
          <span class="badge bg-danger">Rejected</span>
          <p class="small text-muted mt-1 mb-0">Your listing was not approved. You can edit and resubmit.</p>
        </div>
      </div>
    </div>
  </div>
</div>
